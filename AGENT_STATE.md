updated_utc: 2026-09-04T12:30:00Z
repo: poler-os
branch: main
commit: (см. git log — v0.18.0 final: residual-фикс + 3 корневых бага закрыты)
tag: v0.18.0 (ПОСТАВЛЕН — полная e2e-матрица зелёная)
pushed: true
tests: zig build 5/5 шагов; zig build test 335/335 (+6 к 329: sched_resume — пер-таск резюм-кадры .bss: раскладка InterruptFrame из syscall-каскада, frameContentValid-перенос, иммунитет к каскадам, no-op-гарды, 16-выравнивание слотов); E2E QEMU 11.0.2 ПОЛНАЯ МАТРИЦА: pe-run4 (curl HTTP: DNS→TCP→GET→«Example Domain»→ExitProcess(0)) ALL PASS; pe-run7 (HTTPS X25519MLKEM768 без -k: handshake→расшифрованный HTML→ExitProcess(0), scheduler alive) ALL PASS (1 из 2 прогонов — энтропийный флейк bad-decrypt, ретрай зелёный, известный маргинал PUF-посева); pe-run8 (7za b -mmt1 LZMA bench→Exit(0) + ВТОРОЙ процесс curl -o index.html→FAT32 write→cat→ls) ALL PASS — МНОГОПРОЦЕССНОСТЬ (два PE в одном буте) ВПЕРВЫЕ; kernel-panic = 0 во всех прогонах
current_task: CDD-цикл №9 ЗАВЕРШЁН — v0.18.0 ВЫПУЩЕН. Residual tasks[].rsp ЗАКРЫТ + найдены и закрыты ТРИ КОРНЕВЫХ БАГА (см. current_task_note)
current_task_note: ФИНАЛЬНАЯ ВОЛНА v0.18.0 (сессия 2026-09-04): (A) КОРЕНЬ cks-гонки ВСЕХ ВЕРСИЙ НАЙДЕН И ЗАКРЫТ: DEBUG-кадр win32_crt.dispatch ≈ 57КБ (гигантский if-else — ВСЕ локали веток живут в ОДНОМ кадре Debug-сборки) > 32КБ kstack → каскад syscall ПЕРЕПОЛНЯЛ kstack на ~25КБ ВГЛУБЬ kstack СОСЕДА (порча замороженных кадров DNS-буферами — эмпирическая сигнатура v0.14-v0.18.0). ЛЕЧЕНИЕ: Task.kernel_stack 32КБ→128КБ (×8 = 1МБ .bss, identity 4ГБ + user-PML4-копия покрывают). (B) ZIG ERROR-RETURN NULL-CTX CRASH: builtin.returnError при отсутствии trace-контекста читает поля по NULL=физ.0 — реально-режимный BIOS IVT-мусор (F000:FF53) превращался в index/буфер трассы → неканоническая запись → #GP при ЛЮБОМ error-return в kernel-цепочке (эмпирика: второй peload, mapPageInPML4.AlreadyMapped). ЛЕЧЕНИЕ: зануление первой физ. страницы на буте (rep stosb; IVT/BDA после ухода из реального режима не читает никто). (C) МНОГОПРОЦЕССНЫЙ RE-MAP: user-образы (image 0x140000000, стек 0x21…, heap 0x30…) живут в ОБЩЕЙ цепочке PML4[0] (createUserPML4 копирует kernel-записи) — маппинги ЗАВЕРШИВШЕГОСЯ процесса УТЕКАЮТ в общих таблицах → второй peload ловил AlreadyMapped (352 коллизии в pe-run8) → ошибки. ЛЕЧЕНИЕ: remap-толерантность в kernelLoaderOps.map_user И win32_crt-ops kMapUser: unmap старого (unmapPageInPML4 освобождает опустевшие таблицы) + ретрай нового. (D) RESIDUAL tasks[].rsp ЗАКРЫТ АРХИТЕКТУРНО: sched_resume.zig — ПЕР-ТАСК резюм-кадры syscall в .bss (syscall_frame_tab[8][22] align(16) — @alignOf(InterruptFrame)=16, packed struct!): kSleepTask при входе снимает полный InterruptFrame из ЖИВОГО каскада (каскад при 128КБ kstack на СВОЁМ топе), эпилог направляет tasks[owner].rsp+shadow_rsp на слот; taskRspValid-whitelist; FRAME-GUARD семантика «50 ПОДРЯД» (сброс счётчика на живом кадре — резолверы не отсекаются по накопленным пропускам). После (A)-(D): RESUME-SLOT-диспетчеризация = 0 в pe-run4 (кадры валидны на СВОИХ kstack).
next_task: (v0.19-бэклог) process-exit-cleanup: освобождение страниц/PML4/таблиц мёртвого процесса (сейчас старые физ-страницы образа остаются занятыми — remap-модель вместо cleanup; VHEAP/bump-heap ctx тоже не переиспользует память); энтропийный флейк bad-decrypt pe-run7 (1/2 прогонов — PUF-посев маргинален); приватная цепочка PML4[0] для user-VA вместо общей kernel-цепочки (устранит remap-необходимость); SACK+TCP-опции; ELF-загрузчик для Linux-ABI (linux_syscalls.zig готов, dispatcher ждёт первого ELF-процесса); split win32_crt.dispatch (57КБ Debug-кадра — по подфункциям на DLL)
blocked_on: — (v0.18.0 выпущен: матрица зелёная)
tmux_sessions: нет (QEMU 11.0.2 фоном через qemu-portable/qemu-portable.sh; e2e-харнессы scripts/e2e/{e2e_lib,pe-run4,pe-run7,pe-run8,pe-run4-dbg,pe-run4-dump}-e2e.py: serial unix-socket wait=on + monitor sendkey-ввод + CPIO/FAT32-билдеры)
credentials: ВАЛИДЕН — файл-хранилище upload/«гитхаб токен .txt»

## Последние сессии

| Дата (UTC) | Задача | Результат |
|---|---|---|
| 2026-09-04 | **v0.18.0 FINAL: CDD-цикл №9 закрыт — residual + 3 корневых бага + ПОЛНАЯ матрица** | (A) КОРЕНЬ cks-гонки найден: DEBUG-кадр win32_crt.dispatch 57КБ > 32КБ kstack → 25КБ-переполнение в kstack СОСЕДА («DNS-спрей» v0.14-v0.18) → kstack 128КБ. (B) #GP returnError: NULL-ctx читал BIOS IVT (физ.0) → зануление первой страницы. (C) Многопроцессный re-map: утечка маппингов мёртвого процесса в общей цепочке PML4[0] → AlreadyMapped ×352 → remap-толерантность loader+CRT ops → ВТОРОЙ peload работает (7za→curl в одном буте — МНОГОПРОЦЕССНОСТЬ). (D) sched_resume.zig: пер-таск .bss резюм-кадры (align16!) + FRAME-GUARD «50 подряд». 335/335; pe-run4+7+8 ALL PASS; kernel-panic=0. ТЕГ v0.18.0. |
| 2026-09-03 | v0.18.0-RC: CDD-цикл №9 — Per-Task Syscall State + security-харденинг + Linux POSIX-фундамент | Архитектурное закрытие cks-гонки ~90%: asm-вход syscall по ВЛАДЕЛЬЦУ user-RSP (скан kstack/ustack-таблиц — каскад ВСЕГДА на своём kstack; порча чужих стеков DNS-спреем ИСЧЕЗЛА), ur-владельцы (kSleepTask-будильник/exit/callbackDone/CB-регистрация с save-restore для вложенных InitOnce — мост теперь ЗАВЕРШАЕТСЯ корректно), create_thread 5-арг + регистрация тредовых стеков, SAVE-контент-фильтр, FRAME-GUARD-паллиатив (50-тиковый skip). Харденинг: RX-bounds (OOB DMA), validateRange (overflow+ceiling), PMM-rollback (+unmapPageInPML4). Linux POSIX: linux_syscalls.zig (6 syscall, errno-ABI, LinuxOps) + Task.abi + RAX-маршрутизация + R8/R9-asm. 329/329; kernel-panic=0 во всех e2e; 7za-LZMA крутится; RESIDUAL: tasks[].rsp-десинк после эпилога → треды резолвера → v0.18.1 |
| 2026-09-02 | v0.17.0: CDD-цикл №8 — FAT32 RW + .reloc DIR64 + 7-Zip МОМЕНТ ИСТИНЫ №8 + хардинг cks-гонки | МОМЕНТ ИСТИНЫ №8: 7-Zip LZMA-бенчмарк в Ring 3 ПОЛНОСТЬЮ (сжатие+распаковка, ExitProcess(0)) после SYSTEM_INFO-фикса (48Б Win64 — 64Б-версия затирала ret-слот). FAT32 RW: write→ls→cat round-trip на диске (IDT-49 vblk, BPB align(1), 8.3-матчинг, updateDirEntry ext-фикс). .reloc DIR64 (2258 fixups, DYNAMIC_BASE). Wait fast-path ≤20мс (DNS-латентность). Хардинг-волна (5 итераций p5-p7): heap IF-preserving cli/sti (аллокатор из IRQ оставлял IF=1), удаление трёх cks-ИНЖЕКТОРОВ (ресинки при рассинхронизированном current писали ЧУЖОЙ kstack-топ — корень каскада), SCAN-SAVE (кадр → задаче-владельцу kstack), FRAME-HEAL (тень до килла), kill ВИНОВНИКА по kstack кадра (exitCallback убивал innocent-current — #PF треда 5 убивал curl-main!). Остаток (v0.18-бэклог, как запланировано в p4): рассинхрон current в окнах паркинга → флaky-потеря тредов резолвера curl (фаза D pe-run8 / connect pe-run4). 311/311; 7za 5/5; pe-run4 15/16 |
| 2026-09-02 | v0.15.0: CDD-цикл №6 — HTTPS-замыкание + TCP-hardening + ICMP/диагностика + парковка сна | МОМЕНТ ИСТИНЫ №6: ЗАШИФРОВАННЫЙ ИНТЕРНЕТ В RING 3 — TLS 1.3 (X25519, AES-256-GCM) handshake через нашу сеть, зашифрованный GET, РАСШИФРОВАННЫЙ HTML «Example Domain» напечатан в консоли ОС, close_notify, graceful FIN, exit(0); бэклог ПУСТ. Rot-козёл v0.14.0: CV-треды жгли CPU (255K SleepConditionVariableCS) → крипто 40с → сервер FIN; фикс = кооперативная парковка сна (wake_tick + kSleepTask с cli/hti/user_rsp-реставром): крипто 1с, CV 43. TCP: окно приёма от ринга, RTX 32КБ с бэкоффом, keep-alive, FIN-кланг. ICMP ping 4×4 + ifconfig/netstat. strerror_s/_wcserror_s. MLKEM768 bad-decrypt → цикл №7 (X25519 форс). 296/296; pe-run6+5+4 ALL PASS |
| 2026-09-02 | v0.14.0: CDD-цикл №5 — VirtIO-Net + реальный сетевой стек + SSPI/SChannel TLS-движок | МОМЕНТ ИСТИНЫ №5: РЕАЛЬНЫЙ интернет в Ring 3 — ARP/DNS/TCP/HTML «Example Domain» (870Б) из example.com через наш virtio-net (QEMU SLIRP), печать в консоли ОС, exit(0); бэклог ПУСТ. Драйвер (10Б-virtio-hdr, subsystem 0x2E-фикс) + мини-стек (IP-обрезка, отложенные ACKи). SSPI/SChannel SYNTHETIC-TLS (таблица+8 функций, ClientHello/ServerHello/XOR-stream). Wave-A (mutex/bcrypt/ctype/inet_pton/byteswap). Native-qsort 175Б. Разведка: curl=OpenSSL-сборка (ClientHello 1539Б реальный). 288/288; pe-run5 + pe-run4 ALL PASS |
| 2026-09-01 | v0.13.0: CDD-цикл №4 — SocketState/select/send/recv + КРИТИЧЕ-фиксы планировщика | МОМЕНТ ИСТИНЫ №4: ПОЛНЫЙ HTTP-ОБМЕН — send(«GET / HTTP/1.1…», 75Б) → recv(52Б) → «Hello POLER!» в консоли → exit(0); бэклог ПУСТ. SocketState-движок (опции+FD_*-события+авто-сброс), select с перезаписью fd_set, финал-волна печати (SleepEx/_get_osfhandle/MB2WC/WriteConsoleW). Гонка user_rsp (латент v0.12) — флаг in_win32_syscall; атомарный свитч cli; структурная валидация кадров; 32КБ kstack; канарейки. 271/271; pe-run4 + pe-run v1 + kbd + pe-e2e — ALL PASS |
| 2026-09-02 | v0.16.0: CDD-цикл №7 — PUF-энтропия + VFS/FILE-API + IRQ-сетворкер + CMOS-RTC + гонка CV-тредов | МОМЕНТ ИСТИНЫ №7: ВЕРИФИЦИРОВАННЫЙ ПОСТКВАНТОВЫЙ HTTPS БЕЗ -k — X25519MLKEM768 дефолт (bad-decrypt излечен PUF), CA-бандл из initrd-VFS, живая дата CMOS-RTC, HTML «Example Domain», ExitProcess(0); гонка syscall-выхода закрыта (asm 12→2 команд + re-sync cks + shadow-rsp SELF-HEAL); IRQ-воркер 10Гц cli-секции. 301/301; pe-run7+6+4 ALL PASS |
| 2026-09-01 | v0.12.0: CDD-цикл №3 — версия/SSPI/окружение + МОСТ КОЛБЭКА + Win64-треды + Enrollment-Gate | МОМЕНТ ИСТИНЫ №3c: connect(192.0.2.1:80) — curl.exe через 2 НАСТОЯЩИХ Win64-треда (CreateThread → задача планировщика на общем CR3, exit-трамплин ExitThread, WaitFor-сигналы). Мост InitOnce-колбэка (sysretq→launcher→trampoline→syscall #7; ucl_pending-фикс). Native-bsearch/strcmp/strncmp. Enrollment-Gate (bindEnrolled PASSED). Строково-событийная волна (~35 функций). 266/266; pe-run3 ALL PASS + pe-run v1 (цикло-агностичен) + kbd 9/9 + pe-e2e PASS |
| 2026-09-01 | v0.11.0: CDD-цикл №2 — вторая волна Win32/CRT + углубление в main() | win32_crt.zig (Ops-инъекция, 27 тестов), native-стабы memset/memcpy/memmove/strlen (Ring-3-код, исполнение в тестах), block-heap с realloc, GetProcAddress, QPF/QPC (TSC-калибровка), консольные структуры, ленивые argc/argv/iob, валидация user-указателей (userLeafFlags — #PF-паники ядра исключены). МОМЕНТ ИСТИНЫ №2: «curl: error initializing curl library» — текст curl.exe в консоли ОС через fputs/fputc; exit(2) штатно; 34 impl-вызова. ЛАТЕНТНЫЙ БАГ v0.10: 4C 89 C2 = mov rdx,r8 — аргументы ≥2-аргументных impl-функций перепутаны (GCC-эталон + исполнение). Е2Е: pe-run2 ALL PASS + pe-run(v1)/kbd/peinfo зелёные; 196/196 |
| 2026-09-01 | v0.10.0: CDD-цикл №1 — Ring-3 запуск curl.exe | pe_loader.zig (ops-инъекция, посекционные права, TEB/PEB, 15 нативных тестов), win32_stubs v2 (trap/impl/record), win32_api.zig (34 имплементации), syscall #6. МОМЕНТ ИСТИНЫ состоялся: 17 недостающих функций в цепочке (setvbuf, GetProcAddress, WSAStartup, memset…), 20 реализованных вызовов, curl дошёл до main(), штатный exit. Два критических багфикса на пути: GDT/SYSRET-раскладка (CS=0x23/SS=0x1B) и callee-saved RSI/RDI в syscall-трамплине; boot64.S identity без USER-бита (изоляция). E2E: 16 проверок ALL PASS + регрессии kbd 9/9, peinfo PASS |
| 2026-09-01 | v0.9.0: PE/COFF-лоадер + CDD-стабы (п.1 плана) | pe.zig: парсер PE32+ (без аллокаций, @alignOf==1 на всех структурах — фикс @alignCast-паники на непроверенном входе); win32_stubs.zig: стабы 31Б с выравниванием стека (фикс #GP: movaps на rsp%16==0) и указателем-синглтоном (фикс потери счётчиков). РАЗОБЛАЧЕНО: «zig build test» раньше только КОМПИЛИРОВАЛ бинарники (Step.Compile) — падения/паники не ловились; теперь addRunArtifact → 152/152 реально выполняются. Фикстура: curl.exe (PE32+, 22 DLL/274 fn). E2E QEMU PVH+initrd: 16/16 (peinfo/pestubs) |
| 2026-09-01 | v0.7.3 + v0.8.0: аудит коммитов «antigravity» и фикс | Ревизия v0.7.2: энтропия-хаб оставлен, но найден критический баг (bio-пул читал порт 0x60 ДО handleKeyboard → клавиатура мертва в рантайме). v0.7.3: sink на TSC, Set1-таблица переписана (сдвиг на 1), OBF-гард + маскирование PIC (двойная доставка IRQ1), атомики kbd-буфера, timerTickCallback подключён, шелл на kernel-direct вызовах, стек -8 (SysV ABI), virtio-dump восстановлен, cmd_entropy рефактор. v0.8.0: PVH ELF-note из ассемблера (lld не умеет .long в скриптах!), защита Parser.init(0)/findTag, PMM PVH-fallback. E2E 9/9 |
| 2026-08-31 | v0.7.2 Multi-Pool Entropy Hub | Все 4 пула по спеке (Phase, Bus, IRQ, Bio) + UnifiedEntropyHub + авто-регенерация PRNG + команда entropy; 34/34 тестов |
| 2026-08-31 | v0.7.1 + перенос в оргу | poler-os → poler-engine-org (3-й репо консолидации, private); 94a29e0: puf.zig + boot-привязка TSC-энтропии + 11 тестов; тег v0.7.1; Zig 0.14.0 |
| 2026-08-28 | agent-протокол | AGENT.md-стаб + AGENT_STATE.md (канон: POLER-Quantum-RS v0.3.8) |
| 2026-08-27 | VGA/framebuffer | 594305d: fb_ptr32, CR/LF, Zig 0.14 |
