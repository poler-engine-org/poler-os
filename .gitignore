# Сборочные артефакты
zig-kernel/zig-out/
zig-kernel/.zig-cache/
zig-kernel/iso/boot/poler-os64
zig-kernel/src64/testdata
build/
*.iso
disk.img

# Тяжёлые рантайм-выгрузки (не для git)
cachyos-root/pkgcache/
cachyos-root/root/
*.cpio
userspace/init
userspace/sh
userspace/compositor
userspace/test_init
qemu-full/

# e2e-артефакты
scripts/e2e/*.so
scripts/e2e/__pycache__/

# Логи и дампы (форензика остаётся в git-истории, не в worktree)
logs*/
*.log
serial*.log
core.*

# Секреты и локальные файлы
.env
*.token
token*.txt

# Редакторы/ОС
.vscode/
.idea/
.DS_Store
.tex3/
